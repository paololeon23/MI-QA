/**
 * Copia este archivo como gemini.config.local.js y pega tu API key.
 * gemini.config.local.js está en .gitignore (no se publica).
 */
export const geminiConfig = {
  apiKey: "TU_API_KEY_DE_AI_STUDIO"
};
